VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY

# Only for the local seed/verify scripts (scripts/*.mjs) — NOT bundled into
# the app (no VITE_ prefix). The Supabase Auth admin user's credentials.
SUPABASE_ADMIN_EMAIL=admin@example.com
SUPABASE_ADMIN_PASSWORD=your-admin-password
